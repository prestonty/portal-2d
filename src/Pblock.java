public class Pblock extends Block{
    // the picture itself will be a colour
    Pblock(int x, int y, String picName, int rows, int columns) {
        super(x, y, picName, rows, columns);
    }
}
