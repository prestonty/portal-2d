public class Block extends Sprite{
    // the picture itself will be a colour

    Block(int x, int y, String picName, int rows, int columns) {
        super(x, y, picName, rows, columns);
    }
}
